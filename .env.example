# crates.io API token (scoped to led-sectional-core)
# Create at: https://crates.io/settings/tokens
# Usage: source .env && cargo publish -p led-sectional-core --token "$CRATES_IO_TOKEN"
CRATES_IO_TOKEN=cio_your_token_here
